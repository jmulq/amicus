import { OwnershipTransferred } from "../generated/templates/AmicusProfile/AmicusProfile";

export function handleOwnershipTransferred(event: OwnershipTransferred): void {
    // this isn't needed but for some reason I can't call the profile contracts without the template stuff.
}
